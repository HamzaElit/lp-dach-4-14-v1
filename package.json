{
  "name": "rh-2-7-2026-v7",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "watch": "vite build --watch",
    "build": "vite build",
    "stage": "rm -rf dist && mkdir -p dist/assets && cp index.html dist/ && cp -r assets/images dist/assets/ && cp -r assets/fonts dist/assets/ && cp -r assets/css dist/assets/ && cp -r assets/js dist/assets/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "sass-embedded": "^1.99.0",
    "vite": "^8.0.6"
  }
}
